global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'omniai'
    metrics_path: /api/v1/metrics/prometheus
    static_configs:
      - targets: ['host.docker.internal:8000']
    authorization:
      credentials: REPLACE_WITH_SESSION_TOKEN
